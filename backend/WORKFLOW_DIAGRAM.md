# Bulk Update API - Visual Workflow

## 🔄 Complete Request Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Client sends POST request to /api/bulk-update              │
│  Body: Array of Conversation objects                        │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 1: Load Existing Data                                 │
│  ─────────────────────────────────────────                  │
│  • Read mockMessages.json from shared/data/                 │
│  • Parse JSON into conversation list                        │
│  • Log: "Loaded {N} existing conversations"                 │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 2: Process Each Conversation                          │
│  ─────────────────────────────────────────                  │
│                                                              │
│  FOR EACH conversation in request:                          │
│                                                              │
│    ┌──────────────────────────────────────┐                │
│    │ Check: Does conversationId exist?    │                │
│    └─────────────┬────────────────────────┘                │
│                  │                                           │
│         ┌────────┴─────────┐                                │
│         ▼                   ▼                                │
│    ┌─────────┐        ┌─────────┐                          │
│    │   NO    │        │   YES   │                          │
│    └────┬────┘        └────┬────┘                          │
│         │                  │                                 │
│         ▼                  ▼                                 │
│  ┌──────────────┐   ┌──────────────────────┐              │
│  │ INSERT FULL  │   │ MERGE MESSAGES       │              │
│  │ CONVERSATION │   │ ──────────────────   │              │
│  │              │   │ • Get existing msgs  │              │
│  │ • Add entire │   │ • Create messageId   │              │
│  │   object     │   │   set                │              │
│  │ • Count all  │   │ • Skip duplicates    │              │
│  │   messages   │   │ • Append new msgs    │              │
│  │ • Log added  │   │ • Update metadata    │              │
│  └──────────────┘   └──────────────────────┘              │
│                                                              │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 3: Save Updated Data                                  │
│  ─────────────────────────────────────────                  │
│  • Convert conversations to JSON                            │
│  • Write to mockMessages.json with indent=2                 │
│  • Log: "Successfully saved updated mockMessages.json"      │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 4: Run flattenMessages.py                             │
│  ─────────────────────────────────────────                  │
│  • Execute: python shared/scripts/flattenMessages.py        │
│  • Reads: mockMessages.json                                 │
│  • Outputs: flattenedMessages.json                          │
│  • Flattens nested structure for easier API consumption     │
│  • Log: "Successfully flattened messages"                   │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 5: Rebuild FAISS Index                                │
│  ─────────────────────────────────────────                  │
│  • Call: build_and_save_index()                             │
│  • Reads: flattenedMessages.json                            │
│  • Creates embeddings for all messages                      │
│  • Builds FAISS index for semantic search                   │
│  • Saves: faiss.index + faiss_meta.json                     │
│  • Log: "Successfully rebuilt FAISS index"                  │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 6: Return Response                                    │
│  ─────────────────────────────────────────                  │
│  {                                                           │
│    "success": true,                                          │
│    "message": "Conversations updated successfully",          │
│    "conversationsProcessed": 2,                              │
│    "conversationsAdded": 1,                                  │
│    "conversationsUpdated": 1,                                │
│    "messagesAdded": 5                                        │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Message Deduplication Logic

```
Request Message: { messageId: "msg-123", content: "Hello" }
                          │
                          ▼
         ┌────────────────────────────────┐
         │ Is "msg-123" in existing_msgs? │
         └────────────┬───────────────────┘
                      │
              ┌───────┴────────┐
              ▼                ▼
         ┌─────────┐      ┌─────────┐
         │   YES   │      │   NO    │
         │ (Skip)  │      │  (Add)  │
         └─────────┘      └────┬────┘
                               │
                               ▼
                    conversation.messageResponse.append(msg)
                    messagesAdded += 1
```

---

## 🗂️ Data Flow Through Files

```
Request
   │
   ▼
┌──────────────────────┐
│  mockMessages.json   │  ← Original conversation data
│  (Updated by API)    │     with nested messages
└──────┬───────────────┘
       │
       │ flattenMessages.py
       │
       ▼
┌──────────────────────┐
│flattenedMessages.json│  ← Flattened structure for
│  (Auto-generated)    │     easier API consumption
└──────┬───────────────┘
       │
       │ build_and_save_index()
       │
       ▼
┌──────────────────────┐
│   faiss.index        │  ← FAISS vector index for
│   faiss_meta.json    │     semantic search
│  (Auto-generated)    │
└──────────────────────┘
```

---

## 🔄 Conversation Merge Example

### Before Request

```json
mockMessages.json:
[
  {
    "conversationId": "conv-001",
    "messageResponse": [
      { "messageId": "msg-1", "content": "Hello" },
      { "messageId": "msg-2", "content": "Hi there" }
    ]
  }
]
```

### Request

```json
[
  {
    "conversationId": "conv-001",
    "messageResponse": [
      { "messageId": "msg-2", "content": "Hi there" },      ← Duplicate
      { "messageId": "msg-3", "content": "How are you?" }   ← New
    ]
  }
]
```

### After Processing

```json
mockMessages.json:
[
  {
    "conversationId": "conv-001",
    "messageResponse": [
      { "messageId": "msg-1", "content": "Hello" },
      { "messageId": "msg-2", "content": "Hi there" },
      { "messageId": "msg-3", "content": "How are you?" }   ← Added!
    ]
  }
]
```

### Statistics Returned

```json
{
  "conversationsProcessed": 1,
  "conversationsAdded": 0,
  "conversationsUpdated": 1,
  "messagesAdded": 1
}
```

---

## 🎯 Key Decision Points

```
┌─────────────────────────────────────┐
│ For each conversation in request:  │
└─────────────┬───────────────────────┘
              │
              ▼
      conversationId exists?
              │
      ┌───────┴───────┐
      NO              YES
      │                │
      ▼                ▼
  Add full      For each message:
  conversation  messageId exists?
                      │
              ┌───────┴───────┐
              NO              YES
              │                │
              ▼                ▼
          Append msg       Skip msg
          to array      (no duplicate)
```

---

## 🚀 Performance Notes

- **File I/O**: Single read, single write per request
- **Memory**: All conversations loaded into memory
- **Post-processing**: Sequential (flattenMessages → buildIndex)
- **Indexing Time**: Depends on total message count
  - ~100 messages: < 1 second
  - ~1000 messages: 2-5 seconds
  - ~10000 messages: 10-30 seconds

---

## ✅ Success Indicators

```
✓ mockMessages.json updated
✓ flattenedMessages.json regenerated
✓ FAISS index rebuilt
✓ Statistics returned
✓ No errors in logs
```

---

## ❌ Error Handling

```
Error Occurs
     │
     ▼
┌─────────────────────────────┐
│ Where did error happen?     │
└──────┬──────────────────────┘
       │
   ┌───┴────┬──────────┬──────────┐
   │        │          │          │
   ▼        ▼          ▼          ▼
File I/O  Script    Index     Other
         Exec    Building
   │        │          │          │
   └────────┴──────────┴──────────┘
               │
               ▼
    Rollback not automatic
    (mockMessages.json already saved)
               │
               ▼
    Return 500 error with details
    Client should retry request
```

---

This visual workflow helps understand the complete request-response cycle and data transformations performed by the bulk update API.
